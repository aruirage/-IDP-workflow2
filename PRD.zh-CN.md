# NeosAI 日本保险 IDP — 产品需求文档（PRD · 第二期）

## 第 1 章｜文档基础与范围

### 1.1 文档定位

本 PRD 描述第二期增量需求，面向 Z 客户保险金请求场景的落地交付。平台背景、三层架构、操作端入口、案件/文件/节点状态机、角色权限、业务场景四步向导、工作流画布既有节点、上传/集约/导出/我的任务等，均沿用第一期 PRD，不在此重复。

### 1.2 第二期交付范围

本期在 Z 客户保险金请求场景下，补齐不正检测、个人信息脱敏与字段正常值校验三条能力链。账票类型设置沿用第一期五步向导；QR 字段映射仍在 Step2（OCR/QR 读取设定模式切换），于 OCR 抽出节点内执行；Step3 沿用第一期処理ルール，本期仅增量追加正常值范围。

#### 工作流


| 能力       | 说明                                                    |
| -------- | ----------------------------------------------------- |
| 不正检测节点   | 画布新增节点；分 PS 检查、AI 生成检测、模板特征检测三类；先开类别开关再选对象账票（未指定时全账票） |
| 个人信息脱敏节点 | 画布新增节点；对 Step2 标记脱敏的字段执行 Agent                        |
| 业务场景通知扩展 | Step3 通知规则增加不正检测、个人信息脱敏两类对象节点及对应触发事件                  |
| 设置检查     | 画布含上述节点时，至少开启一项可执行配置                                  |


推荐主链：开始→ AI 前处理 → 不正检测 → OCR 抽出 → 个人信息脱敏 → 数据映射 →AI检证 → 自定义函数 → 终了。个人信息脱敏建议置于 OCR 抽出之后。

#### 账票类型设置（增量）


| 步骤          | 本期追加                                          |
| ----------- | --------------------------------------------- |
| Step1 AI 分类 | 上传 template 样张（QR 解析复用，Step2 不再上传）            |
| Step2 AI 读取 | OCR/QR 模式切换；OCR：prompt + 正解；QR：源目录 + 字段映射；脱敏列 |
| Step3 处理规则  | 正常值范围                                         |
| Step5 效果测试  | QR/OCR 读取、后处理、正常值范围综合试读                       |


Step1 AI 分类、Step4 图像处理及 Step2 读取模型弹窗等沿用第一期，不在本期重写。

#### 引擎与运行时


| 能力          | 说明                                                            |
| ----------- | ------------------------------------------------------------- |
| 通用 QR 解析    | 多 QR 源模型（1～6）；样张 AI 解析生成源与映射；于 OCR 抽出节点内执行                    |
| 正常值范围校验     | OCR 抽出节点内，后处理完成后逐字段校验；越界触发 OCR 抽出确认待办                         |
| QR 与 OCR 协同 | 账票 Step2 已配 QR 映射时 OCR 节点 QR 优先；缺码/失败字段 OCR 兜底；未配 QR 映射时纯 OCR |


### 1.3 范围边界


| 类别  | 第二期                                                                                  |
| --- | ------------------------------------------------------------------------------------ |
| 配置端 | 工作流新增不正检测、个人信息脱敏节点；账票类型 Step2（OCR/QR/脱敏）、Step3（正常值范围增量；処理ルール沿用 P1）；业务场景 Step3 通知对象扩展 |
| 操作端 | 不新增独立页面；既有模块随工作流结果变化（见 1.7）                                                          |
| 引擎  | 不正检测、字段正常值范围校验、个人信息脱敏 Agent；QR 解码在 OCR 抽出节点内执行                                       |
| 不含  | 数据映射、AI 检证完整页面（仍为占位）；API 对外实现；通知/邮件通道与待办框架本身（沿用第一期）                                  |


### 1.4 本期里程碑

- 工作流：不正检测、个人信息脱敏节点上线，检视与设置检查就绪
- 账票类型：Step2 OCR/QR 双模式、脱敏列；Step3 正常值范围（処理ルール沿用 P1）；Step5 综合效果测试
- 运行时：OCR 节点内 QR 优先 + OCR 兜底、不正检测三类可配、范围越界进 OCR 抽出确认、脱敏后导出
- 原型已覆盖的菜单、状态与字段规格可研发验收

### 1.5 假设与依赖


| 编号    | 说明                                              |
| ----- | ----------------------------------------------- |
| A-001 | 界面语言为日语；PRD 正文用中文                               |
| A-002 | QR 按内容签名归类 QR 源，不定页面坐标；LIAJ045 等为样张解析时自动识别的行业协议 |
| A-003 | QR 解码、不正检测与个人信息脱敏由 Agent 服务完成                   |
| A-004 | 未在本版重写的功能，研发与验收沿用第一期 PRD                        |


### 1.6 配置职责分离（第二期）


| 能力       | 规则定义                               | 工作流节点职责                          |
| -------- | ---------------------------------- | -------------------------------- |
| QR 字段取值  | 账票类型 Step2 · QR 模式（QR 源目录 + 字段映射）  | OCR 抽出节点内执行；QR 优先，缺码/失败字段 OCR 兜底 |
| OCR 字段读取 | 账票类型 Step2 · OCR 模式（prompt、正解サンプル） | OCR 抽出节点按账票类型开关执行                |
| 不正检测     | 工作流不正检测节点检视（三类开关 + 对象账票）           | 不正检测节点按类别与对象账票执行                 |
| OCR 与后处理 | 账票类型 Step3 · 処理ルール列                | OCR 抽出节点后处理                      |
| 正常值范围    | 账票类型 Step3 同表                      | OCR 抽出节点后处理完成后自动校验               |
| 脱敏字段     | 账票类型 Step2 · OCR 模式 · マスク列         | 个人信息脱敏节点按账票类型开关执行                |


### 1.7 跨模块影响

第二期不新增操作端菜单或待办类型，但工作流新节点会改变既有模块的输入、展示与触发条件。通知与待办是两条链路：通知告知；待办要求人工处理。


| 模块         | 不正检测 / 正常值范围                                        | 个人信息脱敏                                      | 通知（Step3 规则扩展）                           |
| ---------- | --------------------------------------------------- | ------------------------------------------- | ---------------------------------------- |
| 我的任务       | QR 要確認、低置信、范围越界等仍进 OCR 抽出确认待办（沿用第一期待办类型）；执行页可展示越界原因 | 本期不新增脱敏专用待办；Agent 失败时案件可异常，是否进人工确认沿用第一期异常策略 | 通知本身不生成待办；可与待办并行（如 OCR 失败同时发通知 + 产生确认待办） |
| 案件列表 / 详情  | 文件状态可展示不正检测段；节点进度含不正检测                              | 详情展示脱敏后字段/文件（节点已执行时）；无脱敏前后对比页               | 案件状态随节点失败信号汇聚；不因通知改变状态                   |
| 导出         | 导出 OCR 字段含 QR 写入并经后处理的值                             | 导出脱敏后文件与字段值                                 | —                                        |
| 站内信 / 邮件   | 可配置不正检测处理状态 / 处理结果触发       | 可配置 piiMaskStatus 触发                        | 沿用第一期通知投递；本期仅扩展可选对象节点与触发事件               |
| 业务场景 Step3 | —                                                   | —                                           | 对象节点下拉增加不正检测、个人信息脱敏                      |


待办与通知的分工沿用第一期：OCR 抽出确认、AI 检证确认、人工确认、导出确认等待办类型不变；Step3 通知规则在节点事件命中时发送站内信或邮件，不替代待办生成逻辑。

---

## 第 2 章｜术语与词汇表

本章仅收录第二期新增或易混术语。其余术语沿用第一期 PRD。

### 2.1 术语表


| 中文       | 日语/界面   | 定义                                                            |
| -------- | ------- | ------------------------------------------------------------- |
| 不正检测     | 不正検知    | 工作流节点：检测 PS 加工痕跡、AI 生成图像、模板特征不一致；三类检测各自开关并指定对象账票              |
| QR 字段读取  | QR読取    | OCR 抽出节点内能力：扫描 QR 并按 Step2 QR 模式字段映射写值；非独立工作流节点               |
| QR 源     | QRソース   | 账票级逻辑槽位（1～6），对应页上一个物理 QR 解码串；由样张 AI 解析或手工维护                   |
| 取值方法     | 取値方法    | 字段级 QR 分割开关：关闭时全文取值，开启时按分隔符和顺序取值                              |
| QR 顺序    | 順序      | 分割模式下，从 QR 解码串中取第几个片段（1 起）；同一 QR 源内默认按字段顺序自动排列                |
| 正常范围     | 正常範囲    | Step3 与后处理同表配置的可选校验规则；支持最小值、最大值、单位等                           |
| AI マッチング | AIマッチング | Step3 工具栏：为 Step2 已有字段自动生成并反映处理规则；不生成正常条件                     |
| 个人信息脱敏   | 個人情報マスク | 工作流节点：对 Step2 脱敏=ON 的字段检测 PII 并输出脱敏结果                         |
| 账票类型设置   | 帳票タイプ設定 | 配置端账票主数据模块；Step2 含 OCR/QR 双模式与脱敏；Step3 含処理ルール（P1）与正常范围（P2 增量） |


### 2.2 易混术语


| 中文              | 区分要点                                                           |
| --------------- | -------------------------------------------------------------- |
| OCR 模式 vs QR 模式 | Step2 文本读取内段切换；同一文本字段清单；表格信息读取仅走 OCR；AI 自动生成产出不同（prompt vs 映射） |
| QR 映射 vs 処理ルール  | Step2 QR 模式管取值来源；Step3 管 OCR 后处理（日期、照合等），二者独立                  |
| 全文取值 vs 分割取值开关  | 开关关闭时直接写入 QR 解码全文；开关开启时才按区切り文字 split 后按顺序取片段                   |
| 正常范围 vs AI 检证   | 正常范围是单字段规则，在 OCR 抽出节点内执行；AI 检证是跨字段业务规则，在 AI 检证节点执行             |
| QR 配置 vs OCR 抽出 | Step2 QR 模式定义字段如何取 QR 值；OCR 抽出节点决定是否扫描 QR 并写值                  |
| QR 优先 vs OCR 兜底 | 账票 Step2 已配 QR 映射时 OCR 节点先 QR；缺码/失败/歧义字段走 OCR；未配 QR 映射时纯 OCR   |
| 不正检测 vs 前处理     | 前处理管旋转/补正/整列；不正检测管图像真伪与模板一致性；检视 UI 同为「先开类别、再选对象账票」             |


---

## 第 3 章｜数据结构与状态（第二期增量）

实体关系、八种案件状态、节点进度四态、集约关联四态、角色权限均沿用第一期 PRD。本节仅列第二期变更。

### 3.1 账票类型实体增量


| 属性       | 说明                                                                         |
| -------- | -------------------------------------------------------------------------- |
| QR 源目录   | qrSources[]：id、label、sortOrder、matchRule（可选）；id 用于位置编号和字段映射引用，label 仅作内容提示 |
| 字段 QR 映射 | 每个文本字段：type、qrSourceId、extractMethod、delimiter、segmentIndex                |
| 字段正常值范围  | Step3 与后处理同表：启用、最小、最大                                                      |
| 脱敏标记     | Step2 OCR 模式 · 字段行マスク开关                                                    |


同一诊断书账票类型可覆盖有 QR 与无 QR 物理件；运行时由 OCR 抽出节点内 QR 优先 + OCR 兜底协同。

### 3.2 文件工作流段状态增量

在 AI 前处理段与 OCR 抽出段之间增加不正检测段（画布配置不正检测节点且至少开启一类检测时）：


| 文件处理状态 | 触发条件                       |
| ------ | -------------------------- |
| 不正检测中  | 不正检测节点正在执行                 |
| 不正检测成功 | 段结束（含 skip：三类均未开启或不在对象账票内） |
| 不正检测失败 | 执行失败且未转入人工确认               |


未配置不正检测节点或三类均未开启时，AI 前处理完成后直接进入 OCR 抽出段。个人信息脱敏节点不改变文件处理状态枚举。

### 3.3 联级关系增量

不正检测、个人信息脱敏节点的失败与待办信号参与案件状态汇聚，不改变八种案件状态枚举。文件 → 节点汇聚规则对不正检测段与 OCR 抽出段同样适用。

---

## 第 4 章｜用户交互流程（第二期故事）

操作端三入口（新增上传、案件列表、我的任务）分工沿用第一期 PRD。

### 4.1 含 QR 诊断书的标准主路径

```
上传 → 案件集约 → AI前处理 → 不正检测 → OCR抽出 → … → 导出
```

1. 配置端：Step1 上传 template → Step2 OCR 模式定字段 → 切 QR 模式配置映射关系 → Step3 配后处理与范围；工作流插入不正检测节点并配置三类检测与对象账票。
2. 运行时：OCR 抽出节点对已配 QR 映射的账票 QR 优先写值；缺码/失败 → 该字段 OCR 兜底（使用 Step2 OCR 模式 prompt）。
3. 低置信、正常值范围越界、QR 要確認 统一进 OCR 抽出确认待办；歧义时 QR/OCR 对照、字段可编辑。

### 4.2 个人信息脱敏

1. Step2 对姓名、地址等字段手工开启脱敏；工作流插入个人信息脱敏节点。
2. 运行时对脱敏=ON 字段执行 Agent；节点关闭的账票类型 skip。
3. 操作端第二期不单独展示脱敏前后对比页；导出与案件详情按脱敏后结果展示；不新增脱敏专用待办类型。

### 4.3 字段超出正常值范围

1. Step3 点击 AI マッチング 仅为字段匹配处理规则；正常范围由配置人手动设置并保存。
2. OCR 抽出后引擎逐字段校验；越界触发 OCR 抽出确认待办，执行页展示范围说明。
3. 未配置范围的字段不参与；空值由 Step2 必填规则单独处理。

---

## 第 5 章｜全量功能清单

### 5.1 业务场景设置


| 序号  | 功能               | 功能定义                              | 菜单/路由      | 配置/执行 | 本期详写  | 原型状态  |
| --- | ---------------- | --------------------------------- | ---------- | ----- | ----- | ----- |
| 1.1 | 场景列表与 Step1 集约规则 | 场景新建/复制、账票集合、主账票与字段关联             | 业务规则配置     | 配置    | N     | 沿用 P1 |
| 1.2 | Step2 工作流入口与设置检查 | 进入画布；设置检查含不正检测、脱敏节点至少开启一项可执行配置的校验 | 业务场景 Step2 | 配置    | Y（增量） | 已有    |
| 1.3 | Step4 输出字段与发布    | 输出字段勾选、发布门禁                       | 业务场景 Step4 | 配置    | N     | 沿用 P1 |


### 5.2 工作流画布


| 序号  | 功能           | 功能定义                                                    | 菜单/路由      | 配置/执行 | 本期详写  | 原型状态  |
| --- | ------------ | ------------------------------------------------------- | ---------- | ----- | ----- | ----- |
| 2.1 | 工作流画布        | 拖拽编排、连线、条件分支、变量引用                                       | 业务场景 Step2 | 配置    | N     | 沿用 P1 |
| 2.2 | 不正检测节点       | 分 PS 检查、AI 生成检测、模板特征检测三类；先开类别再选对象账票                     | 画布节点检视     | 配置    | Y     | 已有    |
| 2.3 | 个人信息脱敏节点     | 按账票类型开关执行 Agent；对象字段来自账票类型 Step2                        | 画布节点检视     | 配置    | Y     | 已有    |
| 2.4 | OCR 抽出节点（增量） | 内嵌 QR 字段读取（Step2 QR 模式）；后处理完成后执行正常值范围校验；越界触发 OCR 抽出确认待办 | 画布节点检视     | 配置/引擎 | Y（增量） | 已有    |


### 5.3 账票类型设置


| 序号   | 功能                     | 功能定义                                                       | 菜单/路由      | 配置/执行 | 本期详写  | 原型状态  |
| ---- | ---------------------- | ---------------------------------------------------------- | ---------- | ----- | ----- | ----- |
| 3.1  | 账票类型列表管理               | 菜单进入账票类型列表；支持搜索、追加账票类型、选择后进入五步向导；原型默认包含二维码诊断书和体检报告两个账票类型样例 | 业务规则配置     | 配置    | Y     | 已有    |
| 3.1a | 五步向导框架                 | Step1～Step5 入口与既有步骤能力                                      | 业务规则配置     | 配置    | N     | 沿用 P1 |
| 3.2  | Step2 AI 读取（OCR/QR/脱敏） | OCR/QR 模式切换；导出 template → 填清单 → AI 自动生成；マスク列               | 账票类型 Step2 | 配置    | Y     | 已有    |
| 3.3  | Step3 处理规则             | 正常值范围（增量）；処理ルール、AI マッチング沿用 P1（无 QR 列）                      | 账票类型 Step3 | 配置    | Y（增量） | 已有    |
| 3.4  | Step5 效果测试（增量）         | QR/OCR 读取、后处理、正常值范围、マスク 综合试读                               | 账票类型 Step5 | 配置    | Y（增量） | 已有    |


### 5.4 我的任务


| 序号  | 功能         | 功能定义                          | 菜单/路由       | 配置/执行 | 本期详写  | 原型状态  |
| --- | ---------- | ----------------------------- | ----------- | ----- | ----- | ----- |
| 4.1 | OCR 抽出确认待办 | 低置信、QR 要確認、正常值范围越界等；执行页展示越界原因 | 操作端 · マイタスク | 执行    | Y（增量） | 沿用 P1 |
| 4.2 | AI 检证确认待办  | 跨字段检证复核                       | 操作端 · マイタスク | 执行    | N     | 沿用 P1 |
| 4.3 | 人工确认待办     | 完了 / 补件 / 案件中止                | 操作端 · マイタスク | 执行    | N     | 沿用 P1 |
| 4.4 | 导出确认待办     | 导出前确认                         | 操作端 · マイタスク | 执行    | N     | 沿用 P1 |


本期不新增脱敏专用待办类型；个人信息脱敏在节点内自动完成。

### 5.5 通知


| 序号  | 功能     | 功能定义                              | 菜单/路由      | 配置/执行 | 本期详写  | 原型状态  |
| --- | ------ | --------------------------------- | ---------- | ----- | ----- | ----- |
| 5.1 | 通知规则配置 | 对象节点、触发事件、收件人、主题、正文；本期扩展不正检测、脱敏节点 | 业务场景 Step3 | 配置    | Y（增量） | 已有    |
| 5.2 | 站内信    | 运行时按规则匹配并展示通知消息                   | 操作端 · 站内信  | 执行    | Y（增量） | 沿用 P1 |
| 5.3 | 邮件分发   | 通知规则选邮件渠道时自动投递                    | 引擎         | 执行    | N     | 沿用 P1 |


通知与待办分工：通知只告知，不生成待办；待办生成逻辑沿用第一期。

---

## 第 6 章｜功能详细说明

本章与第 5 章五个模块一一对应。各模块仅写第二期增量；未列条目沿用第一期 PRD。每个模块均含：功能点、页面与入口、字段与参数规格、异常与边界。

### 6.01 业务场景设置

#### 功能点

1. Step2 设置检查：画布含不正检测节点时，至少开启一类检测（PS 检查、AI 生成检测、模板特征检测之一）；含个人信息脱敏节点时，至少对一个关联账票开启脱敏执行。
2. 设置检查通过后场景主状态变为可发布；失败时提示首条原因并定位节点。
3. Step1 集约规则、Step4 输出字段与发布门禁沿用第一期，本期不改交互。

#### 页面与入口


| 场景          | 菜单/入口                 | 角色    |
| ----------- | --------------------- | ----- |
| 场景列表        | 业务规则配置 → 业务场景设置       | admin |
| Step1 集约规则  | 场景详情 Step1            | admin |
| Step2 工作流   | 场景详情 Step2 / ワークフロー設定 | admin |
| Step4 输出与发布 | 场景详情 Step4            | admin |


#### 字段与参数规格


| 字段/参数  | 控件  | 必填  | 数据来源与枚举      | 校验与转换        | 下游消费  |
| ------ | --- | --- | ------------ | ------------ | ----- |
| 场景状态   | 只读  | 是   | 草稿、可发布、已发布   | 设置检查通过 → 可发布 | 发布门禁  |
| 关联账票   | 多选  | 是   | 账票类型设置       | 1～30         | 画布、集约 |
| 设置检查结果 | 系统  | —   | 通过 / 失败 + 原因 | 静态校验，不执行节点   | 可发布判定 |


#### 异常与边界


| 场景      | 条件                 | 用户提示 / 处理                           |
| ------- | ------------------ | ----------------------------------- |
| 不正检测全关  | 画布有不正检测节点但三类检测均未开启 | 设置检查阻断；界面文案：不正検知を1件以上有効にしてください      |
| 脱敏节点全关  | 画布有脱敏节点但全部账票关闭     | 设置检查阻断；界面文案：マスクを実行する帳票を1件以上選択してください |
| 检查失败    | 结构/连线/节点配置不合法      | 提示首条原因；定位节点；状态不变                    |
| 关联账票达上限 | 已选 30 个仍尝试追加       | 阻断追加；提示已达上限                         |


### 6.02 工作流画布

#### 功能点

1. 节点库新增不正检测、个人信息脱敏；推荐主链：AI 前处理 → 不正检测 → OCR 抽出 → 数据映射 → AI 检证。
2. 不正检测：分 PS 检查、AI 生成检测、模板特征检测三类；先开类别开关，再选对象账票（多选，未指定时全账票）；输出处理状态与处理结果（passed / reviewRequired）；引擎内部固定风险阈值为 70，不提供配置项。
3. 个人信息脱敏：对 Step2 脱敏=ON 字段执行 Agent；按账票开关 skip；输出 piiMaskStatus。
4. OCR 抽出（增量）：内嵌 QR 字段读取（规则来自账票类型 Step2 QR 模式；已配 QR 映射时 QR 优先，缺码/失败 OCR 兜底）；QR 非空字段默认跳过识别；后处理完成后执行正常值范围校验；越界生成 OCR 抽出确认待办。
5. 不正检测输出处理状态与处理结果（与前处理、OCR 抽出同构）；脱敏节点仅输出处理状态。

#### 页面与入口


| 场景       | 菜单/入口                                        | 角色    |
| -------- | -------------------------------------------- | ----- |
| 工作流画布    | 业务场景 Step2                                   | admin |
| 不正检测检视   | 选中不正检测节点 · 右侧检视                              | admin |
| 个人信息脱敏检视 | 选中脱敏节点 · 右侧检视                                | admin |
| OCR 抽出检视 | 选中 OCR 抽出节点 · 右侧检视                           | admin |
| 跳转账票设置   | OCR 节点 → Step2 OCR 模式 / QR 模式；脱敏 → Step2 マスク | admin |


#### 字段与参数规格


| 字段/参数         | 控件   | 必填  | 数据来源与枚举                                        | 校验与转换                 | 下游消费          |
| ------------- | ---- | --- | ---------------------------------------------- | --------------------- | ------------- |
| PS 检查         | 开关   | 否   | —                                              | 开启后显示对象账票多选           | 运行时 PS 加工痕跡检测 |
| AI 生成检测       | 开关   | 否   | —                                              | 同上                    | AI 生成图像检测     |
| 模板特征检测        | 开关   | 否   | —                                              | 同上                    | 模板特征不一致检测     |
| 对象账票（各类别）     | 多选   | 否   | Step1 关联账票                                     | 未指定时全账票；至少一类 ON（设置检查） | 该类别检测范围       |
| 脱敏 · 账票开关     | 逐行开关 | 否   | Step1 关联账票                                     | 至少一项 ON（设置检查）         | 运行时是否脱敏       |
| 行状态（关闭）       | 只读   | —   | —                                              | マスク OFF               | —             |
| 不正检测处理状态      | 系统输出 | —   | processing / success / failed | 执行成否；不含业务通過判定 | 通知、案件汇聚 |
| 不正检测处理结果      | 系统输出 | —   | passed / reviewRequired       | 模型分数超固定阈值 70 时为 reviewRequired | 条件分岐、人工确认、通知 |
| piiMaskStatus | 系统输出 | —   | processing / success / failed                  | —                     | 通知、案件汇聚       |
| ocrResult     | 系统输出 | —   | passed / reviewRequired                        | 含范围越界                 | 待办生成          |


#### 异常与边界


| 场景            | 条件                  | 用户提示 / 处理                           |
| ------------- | ------------------- | ----------------------------------- |
| 不正检测全关        | 见 6.01 设置检查         | 阻断可发布                               |
| 三类均未命中对象账票    | 已开类别但对象账票为空且场景无关联账票 | 节点 success skip                     |
| 不正检测 Agent 失败 | 模型异常                | 处理状态=failed；可配置通知；是否进人工确认沿用 P1 异常策略 |
| 脱敏全关          | 见 6.01              | 阻断可发布                               |
| 无脱敏字段         | Step2 无 ON 字段       | 节点 success skip                     |
| 范围越界          | 后处理后值不在区间内          | ocrResult=reviewRequired，进我的任务      |


### 6.03 账票类型设置

#### 功能点

1. 沿用第一期五步向导；Step2 / Step3 / Step5 共用 テキスト読取 | テーブル情報読取 双 Tab，字段行对齐。
2. 菜单进入账票类型列表页；页面外壳沿用第一期原型的全宽 Logo 顶栏与左侧菜单；左侧列表参考业务场景设置的侧栏密度，包含标题、折叠按钮、搜索框、类型追加按钮和账票类型卡片，支持折叠为窄栏；未选择类型时右侧显示引导；选择既有类型或追加新类型后，右侧进入五步向导。
3. Step2 OCR 模式（沿用 P1 节奏）：导出 template → 填写項目名/表格列 → AI自動生成 → 产出 prompt + 正解サンプル；表格读取页保留表级抽出条件和列配置表，列内追加マスク开关。
4. Step2 QR 模式（按需）：仅在文本读取页内切换，不进入表格信息读取；与 OCR 模式使用同一文本字段清单，字段删除和排序在两种模式间同步；初始 QR 源目录为空；系统扫描 Step1 template（Step2 不上传）并按可解析 QR 数量生成 QR 源目录；字段侧手工选择 QR 源；取值方法用分割开关表示，关闭时直接使用 QR 全文，开启时才输入分隔符和顺序；顺序界面从 1 开始，同一 QR 源内默认按字段顺序排列，用户手工修改后保留手工值；QR 源目录以映射表上方紧凑来源条展示，不设独立侧栏，不显示横向滚动条；QR1～QR6 是界面显示编号，删除源后后续编号自动前移，文本字段绑定同步迁移；追加源按当前目录末尾继续编号；源内部保留扫描来源键，显示编号变化不改变区域和解码文本；界面不提供 QR 源命名；追加只能从已扫描但未加入目录的 QR 检测结果中选择，最多 6 个，不能无限手工新增空源；每个源可直接拖动样张预览中的 QR 框来重新指定读取区域，再解析默认保留手工区域；LIAJ045 等行业协议在解析时自动识别增强建议。
5. Step3：沿用第一期処理ルール与 AI マッチング；本期增量为正常值范围（正常条件列）；不含 QR 列。
6. Step5：综合试读摘要（QR 源识别、マスク対象、范围越界、要確認）及逐字段卡片（试读值、后处理、范围、マスク対象标记）。

#### 页面与入口


| 场景           | 菜单/入口                                        | 角色    |
| ------------ | -------------------------------------------- | ----- |
| 账票类型列表       | 业务规则配置 → 帳票タイプ設定；进入时默认打开列表第一条账票类型            | admin |
| 类型追加         | 账票类型列表 → 类型追加；追加后进入 Step1                    | admin |
| 既有类型编辑       | 账票类型列表 → 选择账票类型；进入 Step1                     | admin |
| Step1 模板     | 向导第 1 步 · 上传/再上传 template                    | admin |
| Step2 OCR 读取 | 向导第 2 步 · OCR読取 · テキスト/テーブル Tab              | admin |
| Step2 QR 读取  | 向导第 2 步 · テキスト読取 · QR読取                      | admin |
| Step3 处理规则   | 向导第 3 步 · テキスト/テーブル Tab                      | admin |
| Step5 效果测试   | 向导第 5 步 · テキスト/テーブル Tab                      | admin |
| 样张预览器        | 向导左侧预览区；默认适配可见区域，鼠标滚轮缩放，拖拽平移查看超出区域，不使用浏览器滚动条 | admin |
| 工作流快链        | QR 节点 → Step2 QR 模式；OCR 节点 → Step2 OCR 模式    | admin |
| 读取模型设置       | 业务规则配置 → モデル設定；弹窗内配置读取模型与人工确认触发规则            | admin |


#### 字段与参数规格

#### Step2 · 读取模式


| 字段/参数       | 控件   | 必填  | 数据来源与枚举                      | 校验与转换                                                                                   | 下游消费            |
| ----------- | ---- | --- | ---------------------------- | --------------------------------------------------------------------------------------- | --------------- |
| 读取设定        | 轻量切换 | 是   | OCR設定、QR設定                   | 仅テキスト読取内容区内显示，不作为第二层 Tab；テーブル情報読取固定 OCR；同一字段可分别配置 OCR 与 QR，QR 配置存在时执行侧 QR 优先、失败后 OCR 兜底 | 列展示切换           |
| 导出 template | 按钮   | 否   | 与 OCR 共用 Excel 等             | 导入后刷新字段/表格清单                                                                            | 两种模式共享          |
| AI自動生成      | 按钮   | 否   | OCR：生成 prompt+正解；QR 模式不显示该按钮 | Step1 无 template 时 QR 源目录为空                                                             | 字段规则            |
| 字段主键        | 系统生成 | 是   | 字段、表格列创建时生成                  | OCR/QR 两种模式共用同一 fieldId/tableColumnId；改名不改变主键；QR AI 发现新字段时先进入候选新增，不直接生成第二套字段            | OCR/QR/处理规则统一绑定 |


#### Step2 · OCR 模式


| 字段/参数  | 控件    | 必填  | 数据来源与枚举            | 校验与转换                                                   | 下游消费        |
| ------ | ----- | --- | ------------------ | ------------------------------------------------------- | ----------- |
| 項目名    | 输入    | 是   | template 导入或手工     | —                                                       | 字段主键        |
| 項目タイプ  | 下拉    | 是   | string、number、enum | 与 QR 模式同步；界面提示说明三种类型含义：string 为文字列、number 为数值、enum 为候补值 | 类型转换、正常条件候补 |
| プロンプト  | 输入    | 否   | AI 生成或手工           | —                                                       | OCR 抽出      |
| 正解サンプル | 输入    | 否   | AI 生成或手工           | 用于自动规则匹配与读取                                             | 效果测试、训练     |
| マスク    | 行级开关  | 否   | 默认 OFF             | 用于个人信息脱敏对象判定                                            | 脱敏节点        |
| 表格名称   | 输入/只读 | 是   | template 导入或手工     | 每个账票类型 0～10 个表格                                         | 表格结果分组      |
| 表格抽出条件 | 输入    | 否   | AI 生成或手工           | 用于限定抽出哪张表格                                              | 表格 OCR 抽出   |
| 表格列键名  | 输入    | 是   | template 导入或手工     | 每张表 1～10 列                                              | 表格列主键       |
| 表格列类型  | 下拉    | 是   | string、number、enum | 与 QR 模式同步；界面提示说明三种类型含义：string 为文字列、number 为数值、enum 为候补值 | 类型转换、正常条件候补 |
| 表格列提示  | 输入    | 否   | AI 生成或手工           | —                                                       | 表格 OCR 抽出   |
| 表格列样例  | 输入    | 否   | AI 生成或手工           | —                                                       | 效果测试、训练     |
| 表格列必填  | 行级开关  | 否   | 默认 OFF             | 读取结果为空时在确认画面标红                                          | 表格校验        |
| 表格列マスク | 行级开关  | 否   | 默认 OFF             | 用于个人信息脱敏对象判定                                            | 脱敏节点        |
| 操作     | 行级图标  | 否   | —                  | OCR 字段行、QR 字段行、表格列行统一放在最右列；行 hover 时显示删除叉号和拖动排序把手       | 字段维护        |


#### Step2 · QR 模式


| 字段/参数  | 控件    | 必填     | 数据来源与枚举        | 校验与转换                                                                                                                                                                     | 下游消费      |
| ------ | ----- | ------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| QR 源目录 | 紧凑来源条 | 否      | AI 解析 Step1 样张 | 1～6；id 为显示编号和引用键；界面仅显示 QR1～QR6、删除和追加，不提供命名，不出现横向滚动条；删除后后续编号自动前移，绑定到后续 QR 的字段同步迁移；追加按当前目录末尾继续编号，达到 6 个后禁用；区域通过拖动样张预览中的 QR 框修改，预览上不显示未加入候选框，QR 框不使用虚线样式；显示在文本字段映射表上方，不占独立侧栏 | QR 归类     |
| 項目名    | 只读/输入 | 是      | OCR 字段清单       | 与 OCR 模式同一字段主键                                                                                                                                                            | 字段映射      |
| 項目タイプ  | 只读文本  | 是      | OCR 模式定义的字段类型  | 显示在项目名与 QR 源之间；QR 模式不可编辑，字段类型仅在 OCR 模式维护                                                                                                                                  | 类型确认      |
| QR 源   | 下拉    | 否      | qrSources[].id | 下拉仅显示 QR1～QR6，不显示名称；留空表示该字段不从 QR 取值                                                                                                                                       | 字段归类      |
| 取値方法   | 开关    | QR 源非空 | OFF 为全文、ON 为分割 | 关闭时将 QR 解码全文写入字段；开启时右侧显示并启用分隔符                                                                                                                                            | 取值算法      |
| 区切り文字  | 输入    | 分割开启   | 用户输入           | 仅取值方法开启时可输入；按分隔符 split；连续分隔符之间为空段（如 `a$$b` → `a`、空、`b`）；段内空值占位也视为空                                                                                                        | split 分隔符 |
| 操作     | 行级图标  | 否      | —              | 最右列；行 hover 时显示拖动排序把手和删除叉号；OCR/QR 模式共享同一文本字段清单，拖拽落下后更新字段顺序                                                                                                                | 字段维护      |


#### Step3 · 処理ルール


| 字段/参数 | 控件      | 必填  | 数据来源与枚举             | 校验与转换                                                                                                                                                                                   | 下游消费      |
| ----- | ------- | --- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 処理ルール | 下拉+行内参数 | 是   | マスタ照合、日付、金額、テキスト置換等 | 无 QR 类选项；处理规则列第一行固定显示规则下拉，マスタ照合、テキスト置換等规则参数固定显示在第二行；第一行规则下拉宽度与第二行参数区总宽度保持一致；正常条件列隐藏时，处理规则列展开占用剩余空间；不单独设置「設定内容」列                                                                         | OCR 后处理   |
| 正常条件  | 下拉+条件输入 | 否   | 範囲内、<、≤、>、≥、列挙      | 文本字段与表格字段共用同一设计；当前表内至少存在一个字段类型为 number 或列挙的字段时显示整列，不依赖处理规则是否已选择；未选择时表示不配置正常条件；字段类型为 number 时可选范围类和比较符号条件；字段类型为列挙时仅可选列挙并输入候补值；字段类型为 string 时不显示控件；提示说明放在列名；范围类条件只配置条件类型、下限、上限；不与处理规则参数混排 | OCR 后处理判定 |
| 自信度   | 只读/输入   | 否   | 试读结果                | —                                                                                                                                                                                       | Step5 展示  |


日期类只做格式和日期有效性处理，不配置正常条件。AI マッチング 不读取样张基準値列，也不自动生成下限、上限、比较方式或列挙候补；正常条件均由配置人手动设置。病名、判定结果等有限集合字段通过列挙配置允许值。配置端不维护单位字段，执行端通过提示词和标准化逻辑把 OCR 抽出值、报告单位与配置值转换到可比较口径后再判定。执行端进行范围类条件判定时，先对抽出值做数值解析，可兼容「0.1以下」「<0.1」「10 mg/dL」等含比较词或单位的结果；解析失败、单位无法换算或与条件冲突时，不作为系统异常，按置信度与节点设置进入确认画面或条件节点。

读取模型设置的人工确认规则包含：读取置信度低于阈值、多模型结果不一致、主数据照合类异常、正常值范围超出。正常值范围超出时，执行端生成「正常値範囲を超えています」作为确认理由，并进入 OCR 抽出确认或人工确认任务。

#### Step5 · 效果测试

Step5 的试读结果展示 OCR/QR 读取、后处理与正常条件判定结果。字段值来自 QR 时，结果卡片显示对应 QR 来源；字段触发正常值范围越界时，错误理由显示「正常値範囲を超えています」，并同时展示配置的正常范围。テーブル情報読取的异常单元格同样在单元格下方展示正常范围。


| 字段/参数   | 控件   | 必填  | 数据来源             | 说明                                             |
| ------- | ---- | --- | ---------------- | ---------------------------------------------- |
| 试读摘要    | 只读   | —   | 引擎 mock          | QR 源 x/N、マスク対象 n 项、范围越界、要確認                    |
| 后处理路径   | 只读   | —   | 如 QR→日付          | 逐字段                                            |
| 范围结论    | 只读标签 | —   | 範囲OK / 範囲越界 + 区间 | 逐字段                                            |
| マスク     | 只读标签 | —   | マスク対象            | Step2 マスク=ON 字段；配置试读不显示脱敏后值，实际脱敏由工作流个人信息脱敏节点执行 |
| QR 分割预览 | 只读   | —   | 选定 QR 源 split 结果 | 有 QR 映射时                                       |


#### 异常与边界


| 场景               | 条件            | 用户提示 / 处理                 |
| ---------------- | ------------- | ------------------------- |
| Step1 无 template | QR 模式点 AI 生成  | 禁用；提示回 Step1 上传           |
| 样张无 QR           | AI 解析 0 条     | 提示换样张或仅用 OCR 模式           |
| Step1 换 template | 样张变更          | 提示「建议重新 AI 生成」；不自动覆盖已编辑映射 |
| Step2 无字段        | 字段表为空         | QR/OCR AI 禁用；提示先维护字段清单    |
| 范围不完整            | 启用但未填 min/max | 阻断保存 Step3                |
| min > max        | 区间矛盾          | 阻断保存                      |
| AI 匹配失败          | 模型不可用         | 「AI 匹配失败，请稍后重试」           |
| 试读越界             | Step5 测试值超范围  | 卡片展示失败原因；不阻断配置保存          |


#### 待确认


| 编号    | 问题                             | 默认假设            |
| ----- | ------------------------------ | --------------- |
| P-201 | OCR 对已 QR 填充字段是否允许强制覆盖         | QR 非空则跳过 OCR 识别 |
| P-301 | matchRule 是否对配置端可见可编辑          | AI 生成后可读；高级模式可改 |
| P-302 | QR/OCR 冲突时确认页默认展示              | 并列展示；默认 QR 值可编辑 |
| P-303 | Step1 换 template 后是否自动重跑 QR 解析 | 不自动；提示重新 AI 生成  |
| P-207 | AI マッチング 是否覆盖已编辑行              | 默认不覆盖           |


### 6.04 我的任务

#### 功能点

1. 沿用第一期四类待办：OCR 抽出确认、AI 检证确认、人工确认、导出确认。
2. 本期增量：OCR 抽出确认执行页展示读取来源（QR/OCR/兜底）、QR 候选与 OCR 对照、正常值范围越界原因。
3. QR 要確認、低置信、多模型不一致、主数据照合失败等仍进 OCR 抽出确认，入口与类型不变。
4. 个人信息脱敏不新增待办类型；脱敏 Agent 在节点内自动完成。
5. 通知命中不生成待办；待办与通知可并行（如 OCR 失败既通知又产生确认待办）。

#### 页面与入口


| 场景       | 菜单/入口              | 角色        |
| -------- | ------------------ | --------- |
| 待办列表     | 操作端 → マイタスク        | 操作员、操作管理者 |
| OCR 抽出确认 | 待办列表 → 类型筛选 / 打开任务 | 被指派的案件担当者 |
| AI 检证确认  | 同上                 | 同上        |
| 人工确认     | 同上                 | 同上        |
| 导出确认     | 同上                 | 同上        |


#### 字段与参数规格


| 字段/参数  | 控件    | 必填  | 数据来源与枚举                  | 校验与转换                 | 下游消费    |
| ------ | ----- | --- | ------------------------ | --------------------- | ------- |
| 待办类型   | 只读/筛选 | 是   | OCR抽出确认、AI检证确认、人工确认、导出确认 | 第二期不增枚举               | 列表分组    |
| 字段值    | 可编辑   | 依规则 | OCR/QR 抽出结果              | 经办人可修正；歧义时选定 QR 或 OCR | 流程继续    |
| 读取来源   | 只读    | —   | QR / OCR / QR→OCR兜底      | —                     | 列表与详情展示 |
| 越界说明   | 只读    | —   | 账票类型 Step3 范围配置          | 展示 min/max 与实测值       | 复核依据    |
| QR 对照  | 只读    | —   | QR 候选 + OCR 对照           | QR要確認 时并列展示           | 复核依据    |
| 失败原因标签 | 只读    | —   | 低置信 / 范围越界 / QR要確認 等     | OR 汇总                 | 列表与详情展示 |


#### 异常与边界


| 场景    | 条件                   | 用户提示 / 处理             |
| ----- | -------------------- | --------------------- |
| 无待办权限 | 用户非指派角色              | 列表不可见该任务              |
| 字段未修正 | 必填仍空或仍越界             | 阻断办结；提示修正             |
| 脱敏失败  | piiMaskStatus=failed | 不进脱敏待办；走案件异常策略（沿用 P1） |


### 6.05 通知

#### 功能点

1. 沿用第一期 Step3 全局通知规则模板；运行时按节点类型与事件匹配，命中则发送。
2. 本期扩展对象节点：不正检测、个人信息脱敏。
3. 本期扩展触发事件：不正检测处理状态（processing / success / failed）、不正检测处理结果（passed / reviewRequired）、piiMaskStatus（processing / success / failed）。
4. 投递渠道：站内信（必达展示）；邮件（规则选邮件渠道时，沿用 P1 邮件分发）。
5. 通知不替代待办；画布无对应节点时规则保留但不触发、不报错。

#### 页面与入口


| 场景     | 菜单/入口             | 角色        |
| ------ | ----------------- | --------- |
| 通知规则配置 | 业务场景 Step3 · 通知設定 | admin     |
| 规则列表   | Step3 下方通知ルール一覧   | admin     |
| 站内信收件箱 | 操作端 · 站内信         | 操作员、操作管理者 |
| 邮件     | 运行时投递至配置邮箱        | 通知对象      |


#### 字段与参数规格


| 字段/参数   | 控件  | 必填  | 数据来源与枚举                          | 校验与转换                                                | 下游消费  |
| ------- | --- | --- | -------------------------------- | ---------------------------------------------------- | ----- |
| 对象节点    | 下拉  | 是   | 固定节点类型；含不正检测、个人信息脱敏              | 须为画布存在的类型才运行时触发                                      | 规则匹配  |
| 触发事件    | 下拉  | 是   | 依对象节点；不正检测：处理状态 / 处理结果；脱敏：piiMaskStatus | processing / success / failed；passed / reviewRequired | 触发条件  |
| 通知对象    | 多选  | 是   | 操作员、操作管理者                        | 可多选                                                  | 收件人   |
| 通知渠道    | 隐含  | —   | 站内信、邮件                           | 邮件沿用 P1                                              | 投递方式  |
| 件名 / 正文 | 文本  | 是   | 用户输入                             | 支持插入变量 token                                         | 渲染内容  |
| 规则状态    | 开关  | 是   | 开启 / 关闭                          | 关闭则不触发                                               | 运行时匹配 |


#### 异常与边界


| 场景     | 条件                     | 用户提示 / 处理         |
| ------ | ---------------------- | ----------------- |
| 节点已删除  | 规则引用节点类型不在画布           | 保存时提示失效并须重选       |
| 画布无节点  | Step3 配了某节点但 Step2 无实例 | 运行时不发送、不报错        |
| 变量缺值   | 正文变量在本案路径无值            | 按空值渲染；不阻断流程       |
| 邮件投递失败 | SMTP 等异常               | 记录日志；工作流继续（沿用 P1） |


---

