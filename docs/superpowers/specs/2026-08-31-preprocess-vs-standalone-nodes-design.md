# 前处理模块化架构（已定稿）

日期：2026-08-31  
状态：**已定稿** — 仅采用前处理模块化，不增独立画布节点  
主文档：`PRD.zh-CN.md` **§1.3 工作流架构（前处理模块化）**

---

画像不正検知、敏感情报检出并入前处理节点，与画像回転/補正/整列同构（模块开关 + 对象账票）。节点输出仅 `preprocessStatus`、`preprocessResult`。执行顺序：不正検知 → 脱敏 → 回転 → 補正 → 整列。**案件一覧 ファイルステータス 沿用 P1（仅 前処理 / OCR抽出 段），不展示子模块段。**

独立节点方案（fraud_detect / pii_mask 画布节点）已废弃，不再作为备选。

引擎 sub-step 上报等实现细节见 PRD §1.3 表格 E1～E3 与第 3 章文件状态。
