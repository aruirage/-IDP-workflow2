# LIAJ 诊断书 QR —— 分割规则与读取算法规范

适用帐票：`LIAJ045-A01` / `LIAJ045-A04` / `LIAJ001-K01`
验证数据：6 名患者的真实 QR（安达珠美・平田よし子・藤岛纱织・土谷胜・服部朱理・塚原明夫）

---

## 0. 结论（先看这里）

> **QR 不是「一个字段 = 一个二维码」。**
> 它把**一整个长字符串（payload）按尺寸 N 等分**，再塞进各个 QR。
> 所以**切点跟字段边界无关**，字段会横跨两个 QR。

> ※ 本规范**只处理「多个 QR」的情况**（A01 / A04 = 6 个，K01 = 9 个）。
> 单张 QR 的情况不在考虑范围内。

**读取铁律：**

```
QR1 ‖ QR2 ‖ … ‖ QRn  →（拼接）→  payload.split('$')
```

**绝对不要「逐个 QR 分割」。** 这是重复行 / 数据丢失的唯一原因。

---

## 1. QR 的整体结构

一页里混着**数据 QR 群**和**ID 类 QR**。

| 类型 | 内容 | 实例（实测） | 判别 |
|---|---|---|---|
| **数据 QR** | payload 的片段，**必定含 `$`** | `040c1$001$LIAJ045-A01-202104^安達　珠美$女$…` | 含 `$` |
| ID 类 QR① | 上传 ID | `00000000-000_00000000-000-000_HE03-001_00` | 含 `HE03` |
| ID 类 QR② | 受理类 ID | `A33343002.A` | 不含 `$` |

### 实测布局

| 帐票 | 数据 QR 数量 | 排列 | ID 类 QR 位置 |
|---|---|---|---|
| LIAJ045-A01 | 6 | 底行单排（从左到右） | 左上（`HE03`） |
| LIAJ045-A04 | 6 | 底行单排（从左到右） | 左上（`HE03`） |
| LIAJ001-K01 | 9 | 底行单排（从左到右） | 上部（`A33343002.A`） |

实测坐标（归一化 y, x）：

```
安达珠美  ： 数据 6 个，全部 y≈0.90 / ID 类 y≈0.03
塚原明夫  ： 数据 9 个，全部 y≈0.885 / ID 类 y≈0.05
```

> ⚠️ 一次解码不一定能扫全（塚原 9 个里有 1 个在低分辨率下漏掉）。
> 要**用多个缩放倍率 / 分辨率分别解码后再去重**。

---

## 2. 「尺寸」是什么（白话版）★最重要

### 2.1 一句话：尺寸 = 这串字「占多宽」

想象你在用**等宽字体**打字（打字机、记事本、代码编辑器那种，每个字母一样宽）：

| 打什么 | 占几格 |
|---|---|
| 数字 `0-9`、字母 `A-Z a-z`、半角符号 `$ ^ ! - ( ) ,`、半角空格 | **1 格** |
| 日文汉字、平假名、片假名、全角符号 `　`（全角空格）、`（）` `｢｣` | **2 格** |

**「尺寸」就是这串字一共占多少格。**

### 2.2 为什么不能用「字数」

```
1234567890   → 10 个字，占 10 格
あいうえお    →  5 个字，占 10 格   ← 字数少一半，宽度却一样
```

**字数差一倍，宽度一样。** 所以按字数切和按宽度切，结果完全不同。

### 2.3 在电脑里，它正好等于 Shift-JIS 的字节数

| | 尺寸 | Shift-JIS 字节数 |
|---|---|---|
| 日文 1 个字 | 2 | 2 |
| 数字 1 个字 | 1 | 1 |
| `$` `^` | 1 | 1 |

→ **尺寸 = 把字符串存成 Shift-JIS 时的字节数。** 代码里直接能算：

```python
len(s.encode('cp932'))             # Python
```
```js
Buffer.byteLength(s, 'shift_jis')  # Node.js
```

### 2.4 实测例子（安达珠美的 QR1）

```
040c1$001$LIAJ045-A01-202104^安達　珠美$女$0000214951$1960$11$9$上行結腸癌$C182$$1$$1$$$$不詳$$$$不詳$$$$$$2026$5$18$$$$$1$2026$5$28$20
```

| | 个数 | 尺寸 |
|---|---|---|
| 半角（数字・`$`・`^`・字母） | 105 | 105 × 1 = **105** |
| 全角（`安達　珠美` `女` `上行結腸癌` `不詳` `不詳`） | 15 | 15 × 2 = **30** |
| **合计** | **120 个字** | **135 格** |

### 2.5 为什么这件事重要

打码的那一端是**按宽度平均分**的，不是按字数分：

```
安达整串的总尺寸 = 809 格
6 个二维码  →  每个大约 809 ÷ 6 ≈ 135 格
```

135 格能装 **135 个数字**，或者 **67 个日文字**。

它切在「第 135 格」的位置上 —— 这个位置可能正好落在某个日文字 / 某个字段的中间，
于是就切开了它：

```
…$28$20  |  26$6$11$…     ← 2026 被切成 20 + 26
…不       |  詳$…           ← 不詳 被切成 不 + 詳
```

### 2.6 验证：只有「尺寸」解释得通

33 个切点的误差率：

| 度量 | 最大误差率 |
|---|---|
| 字数 | 6.5% |
| UTF-8 字节数 | 3.5% |
| `$` 权重 0.5 | 6.4% |
| **尺寸（= Shift-JIS 字节数）** | **2.08%** ✅ 最优 |
| `$` 权重 1.25 | 3.5% |
| `$` 权重 0（完全不算 `$`） | 24.6% |

只有尺寸能让 6 个二维码的宽度几乎相等（都在 ±6% 以内）。
字符数是 ±15%，token 数是 ±50%。

→ **尺寸是唯一正确的度量。**

---

## 3. 分隔符（两个层级）

| 层级 | 字符 | 作用 | 出现位置 |
|---|---|---|---|
| 第 1 层 | `$` | 字段分隔 | 全文（恒定） |
| 第 2 层 | `^` | 子分隔 | **仅第 3 个 token**（模板ID^患者姓名） |
| 转义 | `!s` | 1 个半角空格 | **仅 K01**（3.病理組織診断名） |

### 关于 `^`

- 6 名患者里 **`^` 各只出现 1 次**，必定在第 3 个 token（0-based index 2）内。
- 因此第 3 个 token 要**展开成 2 个字段**。

| 帐票 | `^` 在 payload 内的偏移 |
|---|---|
| LIAJ045-A01 / A04 | 28 |
| LIAJ001-K01 | 33 |

### 关于 `!s` 转义

- `!` 后面**全部 7 处都是 `s`**（不存在 `!s` 以外的形式）。
- `!s` → 半角空格 ` `。
- 只出现在**塚原明夫（K01）的「3.病理組織診断名」**，其余 5 人为 0 次。

```
'Non-invasive!spapillary!surothelial!scarcinoma,!shigh!sgrade,!surinary!sbladder,'
  → 'Non-invasive papillary urothelial carcinoma, high grade, urinary bladder,'
```

> ⚠️ `,` 是**未被转义的普通逗号**，不是 `!,`。

### ⚠️ 半角空格（U+0020）是「普通字符」

它作为字段值的内容正常出现。**既不是分隔符，也不是转义。**

| 出现位置 | 实例 |
|---|---|
| 藤岛纱织的**患者姓名** | `藤島 紗織` ← 半角空格（其余 5 人是全角 `安達　珠美`） |
| 英文病理组织诊断名 | `Adenocarcinoma of the ascending colon` |
| 医疗机构名 | `公益財団法人 大原記念倉敷中央医療機構　倉敷中央病院` |
| 药剂名 | `アンペック坐剤 10mg` |

**患者姓名的分隔符全角 / 半角混用。** 要做名寄（同一人合并）必须做规范化。

---

## 4. 帐票识别

### 4.1 帐票码与模板 ID

| 帐票码 | 模板 ID | 帐票枝番 | QR 数量 | 拼接后 token 数 |
|---|---|---|---|---|
| `040c1` | `LIAJ045-A01-202104` | `001` | **6** | **406** |
| `040c2` | `LIAJ045-A04-202104` | `001` | **6** | **301** |
| `040215(04)` | `LIAJ001-K01-202604` | `009` | **9** | **291** |

### 4.2 模板 ID 的结构

```
LIAJ045 - A01 - 202104
   │       │      └─ 修订年月
   │       └─ 帐票版本（枝番）
   └─ 样式码
```

### 4.3 帐票码的格式

```
^\d{5,6}(\(\d+\))?$     例: 040c1 / 040c2 / 040215(04)
```

### 4.4 ★QR 数量 N 由「帐票定义」固定，不要用尺寸去算

决定性反例：

| 帐票 | 总尺寸 | QR 数量 |
|---|---|---|
| LIAJ045-A01（藤岛） | 638 | 6 |
| LIAJ045-A01（平田） | 820 | 6 |
| LIAJ001-K01（塚原） | **719** | **9** |

→ K01 比 A01 **还小**，却要 9 个。**任何单一尺寸阈值都无法解释。**
→ **把 N 当作帐票规格里写死的常量。**

---

## 5. QR 分割的规则

### 规则 R1 —— payload 是一条连续字符串

```
payload = QR1 + QR2 + … + QRn     （字符串拼接，无重复、无缺失）
```

### 规则 R2 —— 分割单位是「尺寸」（见 §2）

### 规则 R3 —— 切点落在「尺寸的等分点」上

> **第 k 个切点的位置 ≈ 总尺寸 T × k / N**（k = 1 … N−1）
> 切点个数 = **N − 1**

实测（误差 = 实测 − 理论）：

| 患者 | N | T | 理论位置 | 实测位置 | 最大误差率 |
|---|---|---|---|---|---|
| 安达珠美 | 6 | 809 | 134.8 / 269.7 / 404.5 / 539.3 / 674.2 | 135 / 267 / 404 / 531 / 675 | 1.0% |
| 平田よし子 | 6 | 820 | 136.7 / 273.3 / 410.0 / 546.7 / 683.3 | 139 / 274 / 409 / 535 / 685 | 1.4% |
| 土谷胜 | 6 | 740 | 123.3 / 246.7 / 370.0 / 493.3 / 616.7 | 127 / 252 / 381 / 503 / 621 | 1.5% |
| 服部朱理 | 6 | 611 | 101.8 / 203.7 / 305.5 / 407.3 / 509.2 | 101 / 208 / 304 / 409 / 519 | 1.6% |
| 藤岛纱织 | 6 | 638 | 106.3 / 212.7 / 319.0 / 425.3 / 531.7 | 102 / 211 / 321 / 431 / 542 | 1.6% |
| 塚原明夫 | 9 | 719 | 79.9 / 159.8 / 239.7 / 319.6 / 399.4 / 479.3 / 559.2 / 639.1 | 87 / 161 / 227 / 309 / 390 / 467 / 544 / 635 | 2.1% |

**平均误差 0.76% / 最大 2.08%**（最多 15 字节 ≈ 7 个全角字）

### 规则 R4 —— 切点必定落在「字符边界」

- 不会把一个日文字切成两半（不会切在 2 字节字符中间）
- **但会毫不犹豫地切开字段**

实测到的「字段被切断」共 11 处：

| 患者 | 前一 QR 末尾 | 后一 QR 开头 | 还原值 | 字段 |
|---|---|---|---|---|
| 安达珠美 | `20` | `26` | `2026` | 3.入院期間①開始_年 |
| 安达珠美 | `便潜` | `血陽性` | `便潜血陽性` | 6.前医 病名 |
| 安达珠美 | `上行結` | `腸腫瘍` | `上行結腸腫瘍` | 10.本人に告げた病名 |
| 土谷胜 | `…モルヒネ塩酸` | `塩注100mgシリンジ…` | `…モルヒネ塩酸塩注100mg…` | 12.⑤ 薬剤名 |
| 平田よし子 | `不` | `詳` | `不詳` | 2.ア 傷病発生年月日 |
| 服部朱理 | `8` | `882` | `8882` | 6.手術1 手術コード |
| 藤岛纱织 | `不` | `詳` | `不詳` | 2.ア 傷病発生年月日 |
| 塚原明夫 | `非浸潤性膀胱が` | `ん` | `非浸潤性膀胱がん` | 2.ア 傷病名 |
| 塚原明夫 | `…high!sgrade,` | `!surinary!sbladder,` | `…high grade, urinary bladder,` | 3.病理組織診断名 |
| 塚原明夫 | `…電解質溶` | `液利用のもの` | `…電解質溶液利用のもの` | 7.手術名 |
| 塚原明夫 | `たかだウロ` | `クリニック` | `たかだウロクリニック` | 9.前医 医療機関名 |

> 切点落在 `$$$$$$$$$$`（连续空字段）时，表面上「看不出被切断」。
> 安达的 5 个切点里，**3 个是真实切断**，另 2 个落在空字段区。

### 规则 R5 —— 切点因人而异

```
帐票定义  →  N 固定（A01 = 6 个）
              ↓
患者数据  →  各字段值长度不同 → 总尺寸 T 不同（809 / 820 / 638 / 740）
              ↓
切点 = T × k / N  →  每个人位置都不一样（39 / 35 / 17 / 38 …）
```

**同一个模板 ID 下，患者不同，`(QRソース, 取得位置)` 也不能共用。**

| 能否共用 | 对象 |
|---|---|
| ✅ 可共用 | 字段名・字段排列顺序（A01 的 406 项 4 人共通） |
| ❌ 不可共用 | **QRソース（第几个 QR）・取得位置（该 QR 内第几个）** |
| ✅ 可共用 | **拼接后的 token index（406 / 301 / 291）← 与患者无关** |

### 预测式（哪些字段会被切断）

> 累计尺寸到达 `T × k / N` 的位置：
> - 落在某字段值的**内部** → 该字段被切断
> - 落在 `$$$$` 的**空字段区** → 表面上看不出切断

> ⚠️ 但**精确切点无法预测**。我验证了「尺寸等分」「字符数等分」「token 数等分」
> 「QR 位长（混合模式最优分割）等分」「贪心填满容量」5 种模型，
> 33 个切点里**最多只有 3 个能完全命中**。误差 ≤2% 说明方向对，但生成端内部逻辑不明。
> **不要预测，直接拼接才是唯一安全的做法。**

---

## 5.5 三个版式都适用吗（逐个验证）

**结论：是。A01 / A04 / K01 三个版式全部适用同一套规则。**

### 共通部分（3 个版式完全一致）

| 规则 | A01 | A04 | K01 |
|---|---|---|---|
| payload 连续，拼接可还原 | ✅ | ✅ | ✅ |
| 分割单位 = 尺寸（SJIS 字节） | ✅ 最优 | ✅ 最优 | ✅ 最优 |
| 切点 = 尺寸等分点 `T×k/N` | ✅ ≤1.6% | ✅ 1.6% | ✅ 2.1% |
| 切点落在字符边界、会切开字段 | ✅ | ✅ | ✅ |
| `^` 只出现在第 3 个 token（index 2） | ✅ | ✅ | ✅ |
| `$` 是字段分隔 | ✅ | ✅ | ✅ |
| 空字段不压缩 | ✅ | ✅ | ✅ |

### 差异部分（只是参数不同，规则一样）

| | A01 | A04 | K01 |
|---|---|---|---|
| 帐票码 | `040c1` | `040c2` | `040215(04)` |
| QR 数量 N | **6** | **6** | **9** |
| 拼接后 token 数 | **406** | **301** | **291** |
| 患者间 token 数是否恒定 | ✅ 4 人全是 406 | （1 人） | （1 人） |
| 尺寸离散度 | 平均 3.6% | 5.4% | 7.1% |
| 年号 | 西历 | 西历 | 令和 / 昭和 |
| `!s` 转义 | 无 | 无 | **有**（7 处） |
| 字段布局 | 与 A04 共享约 24% | 同左 | **完全不同** |

### 各版式的最优度量

| 版式 | 患者 | 字数 | 尺寸（SJIS） | UTF-8 | 最优 |
|---|---|---|---|---|---|
| A01 | 安达珠美 | 2.07% | **1.03%** | 1.47% | 尺寸 |
| A01 | 平田よし子 | 2.73% | **1.42%** | 1.66% | 尺寸 |
| A01 | 藤岛纱织 | 6.48% | **1.62%** | 2.42% | 尺寸 |
| A01 | 土谷胜 | 4.59% | 1.49% | **1.48%** | 平手 |
| A04 | 服部朱理 | 3.79% | **1.61%** | 2.31% | 尺寸 |
| K01 | 塚原明夫 | 2.32% | **2.12%** | 3.54% | 尺寸 |

→ **6 人中 5 人明确是尺寸最优，剩下 1 人（土谷）尺寸和 UTF-8 打平。**
→ 没有出现「某个版式不适用尺寸」的情况。

---

## 6. 读取算法

### 6.1 步骤

> ※ 只处理**多个 QR** 的情况（N ≥ 2），所以**拼接是必需的**，不存在「只有 1 个 QR 不用拼」的分支。

```
输入: 已解码的 QR 列表 [{x, y, text}, …]

 1. 筛出数据 QR
      只保留 text 含 '$' 的
      （排除含 'HE03' 的 ID 类、'A33343002.A' 等 ID 类）

 2. 确定所在行
      按 y（取整）分组 → 取 QR 数量最多的那一行
      （数据 QR 排在底行单排）

 3. 从左到右排序
      x 升序

 4. ★拼接（关键就在这一步）
      payload = texts.join('')

 5. 第 1 层分割
      tokens = payload.split('$')

 6. 头部校验
      tokens[0]  : /^\d{5,6}(\(\d+\))?$/     … 帐票码
      tokens[1]  : /^\d{3}$/                 … 帐票枝番
      tokens[2]  : '…^…'                     … 模板ID^患者姓名

 7. 帐票判定 + ★数量校验
      [templateId, patientName] = tokens[2].split('^')
      spec = SPECS[templateId]
      assert spec ≠ null
      assert tokens.length == spec.tokenCount     ← 406 / 301 / 291
      （不一致 = 「QR 漏扫」或「顺序错」或「不是这个帐票」）

 8. 字段映射
      for i in 0..tokens.length-1:
          if i == 2:
              fields['テンプレートID'] = templateId
              fields['患者氏名']     = patientName
          else:
              fields[spec.names[i]] = unescape(tokens[i])

 9. 解除转义
      unescape(v) = v.replace(/!s/g, ' ')
      （'!' 只会以 '!s' 形式出现，所以全局替换是安全的）

输出: { 字段名 → 值 }   ※ 空字符串也要保留（不要压缩）
```

### 6.2 参考实现（JavaScript）

```js
const SPECS = {
  'LIAJ045-A01-202104': { docCode: '040c1',      qrCount: 6, tokenCount: 406, names: NAMES_A01 },
  'LIAJ045-A04-202104': { docCode: '040c2',      qrCount: 6, tokenCount: 301, names: NAMES_A04 },
  'LIAJ001-K01-202604': { docCode: '040215(04)', qrCount: 9, tokenCount: 291, names: NAMES_K01 },
};

const unescapeValue = (v) => v.replace(/!s/g, ' ');

/**
 * @param {{x:number,y:number,text:string}[]} qrs  已解码的 QR
 * @returns {{templateId:string, patientName:string, fields:Record<string,string>}}
 */
function decodeLiajPayload(qrs) {
  // 1. 只要数据 QR（含 '$'，排除 ID 类）
  let data = qrs.filter(q => q.text.includes('$') && !q.text.includes('HE03'));
  if (!data.length) throw new Error('找不到数据 QR');

  // 2. QR 最多的那一行 = 数据行
  const rows = new Map();
  for (const q of data) {
    const key = Math.round(q.y * 50);           // 粗略取整来判定行
    rows.set(key, [...(rows.get(key) ?? []), q]);
  }
  data = [...rows.values()].sort((a, b) => b.length - a.length)[0];

  // 3. 从左到右
  data.sort((a, b) => a.x - b.x);

  // 4. ★先拼接，再分割
  const payload = data.map(q => q.text).join('');
  const tokens  = payload.split('$');

  // 5. 头部校验
  if (!/^\d{5,6}(\(\d+\))?$/.test(tokens[0])) throw new Error(`帐票码非法: ${tokens[0]}`);
  if (!/^\d{3}$/.test(tokens[1]))             throw new Error(`帐票枝番非法: ${tokens[1]}`);
  if (!tokens[2].includes('^'))               throw new Error('第3个 token 里没有 ^');

  // 6. 帐票判定 + 数量校验
  const [templateId, patientName] = tokens[2].split('^');
  const spec = SPECS[templateId];
  if (!spec) throw new Error(`未支持的模板 ID: ${templateId}`);
  if (tokens.length !== spec.tokenCount) {
    throw new Error(
      `token 数不一致: 期望 ${spec.tokenCount} / 实际 ${tokens.length}（可能漏扫 QR）`
    );
  }

  // 7. 字段映射
  const fields = {};
  tokens.forEach((v, i) => {
    if (i === 2) {
      fields['テンプレートID'] = templateId;
      fields['患者氏名']       = patientName;
    } else {
      fields[spec.names[i]] = unescapeValue(v);
    }
  });

  return { templateId, patientName, fields };
}
```

### 6.3 「不拼接、逐个 QR 读」的等价实现

如果不想做字符串拼接，把**下一个 QR 的第 1 个 token 接到前一个的最后一个 token 上**，数学上完全等价。

```js
function tokensFromPerQr(qrTexts) {
  const out = [];
  qrTexts.forEach((text, qi) => {
    const parts = text.split('$');
    parts.forEach((p, pi) => {
      if (qi > 0 && pi === 0) out[out.length - 1] += p;   // ← 接到前一个末尾
      else out.push(p);
    });
  });
  return out;   // 长度会变成 406 / 301 / 291
}
```

验算：安达的原始 token 411 个 − 拼接 5 次 = **406** ✅（与拼接方式完全一致）

> ⚠️ 把拼接后的 index 逆映射回 `(QRソース, 取得位置)` 时，**会有 N−1 个 index 横跨两个 QR**
> （安达是 5 个）。UI 上看起来就是「同一个字段名出现两行」。
> 这正是「第 41 行和第 42 行都显示 `3.入院期間①開始_年`」的真相。

---

## 7. 验证锚点（实测已确认）

### LIAJ045-A01（安达珠美 / 406 token）

| index | 字段 | 安达 | 平田 | 藤岛 | 土谷 |
|---|---|---|---|---|---|
| 4 | 性別 | 女 | 女 | 女 | 男 |
| 6 | 生年月日_年 | 1960 | 1948 | 1992 | 1957 |
| 9 | 傷病名 | 上行結腸癌 | 肺大細胞神経内分泌癌 | 妊娠38週急産予防、分娩後異常出血 | 膵癌 |
| 134 | 7.手術 種類11 | 1 | — | — | — |
| 192 | 10.病理組織診断名 | Adenocarcinoma of the ascending colon | NSCLC with neuroendocrine differentiation | — | 前医に診断 |
| 208/209/210 | 10.TNM分類 T/N/M | 3/1/0 | 3/—/— | — | — |
| 396 | 16.転帰 | 継続 | 継続 | 継続 | 中止 |
| 403 | 郵便番号 | 241-0002 | — | — | — |
| 405 | 医療機関名 | 医療法人社団恵生会　上白根病院 | 製鉄記念八幡病院 | 倉敷中央病院 | 東葛病院 |

### LIAJ045-A04（服部朱理 / 301 token）

| index | 字段 | 值 |
|---|---|---|
| 95 | 6.手術1 手術名 | 子宮附属器腫瘍摘出術（両側）（腹腔鏡） |
| 98 | 6.手術1 手術コード | `8882`（QR3 末尾 `8` + QR4 开头 `882`） |
| 102 | 6.手術 病理組織診断名 | Mature teratoma |
| 121 | 6.手術 種類11 | 1 |
| 216-218 | 15.通院治療 | 2026 / 5 / 21 |
| 295-297 | 発行日 | 2026 / 7 / 20 |
| 298 | 郵便番号 | 471-8513 |
| 300 | 医療機関名 | トヨタ記念病院 |
| 301 | 診療科 | 産婦人科 |

### LIAJ001-K01（塚原明夫 / 291 token）

| index | 字段 | 值 |
|---|---|---|
| 7-8 | 生年月日（年号/年） | 昭和 / 42 |
| 39 | 告知有 | 1 |
| 45 | 今回の区分_原発 | 1 |
| 48 | 病理組織検査 有 | 1 |
| 50 | 病理組織診断名 | Non-invasive papillary urothelial carcinoma, high grade, urinary bladder,（`!s` 已还原） |
| 52 | 非浸潤癌・上皮内癌 | 1 |
| 95 | 転帰 退院 | 1 |
| 112 | 7.手術 有 | 1 |
| 114 | (K)区分番号 | 8036ｲ |
| 115 | 7.手術名 | 膀胱悪性腫瘍手術　経尿道的手術　電解質溶液利用のもの |
| 124 | 手術箇所 | サ |
| 151 | 9.前医 医療機関名 | たかだウロクリニック |
| 287 | 郵便番号 | 101-8309 |
| 289 | 電話番号 | 03-3293-1711 |
| 290 | 病院名 | 日本大学病院 |
| 291 | 診療科 | 泌尿器科 |

---

## 8. 踩坑清单

| # | 坑 | 对策 |
|---|---|---|
| 1 | **逐个 QR 做 `split('$')`** → 字段被切断、重复行 | **必须先拼接再分割** |
| 2 | 用总尺寸去算 QR 数量 | 用**帐票定义里的常量**（R4） |
| 3 | 把全角/半角都当 1 个字符 | **全角 2 / 半角 1**（SJIS 字节） |
| 4 | 忽略 `^` | 第 3 个 token 要**展开成 2 个字段** |
| 5 | 不还原 `!s` | `.replace(/!s/g, ' ')` |
| 6 | 把半角空格误当分隔符 | 它是字段内容。藤岛的姓名里也有 |
| 7 | 把空字段压缩掉 | **空字符串也是 1 个字段**，压缩会让 index 错位 |
| 8 | 把 ID 类 QR 混进来 | 只要含 `$` 的。排除 `HE03` / `A33343002.A` |
| 9 | QR 漏扫 | 用 **token 数校验**（406 / 301 / 291） |
| 10 | 出现两行同名字段 | 正常。落在拼接边界上的字段（N−1 个） |
| 11 | 把不同帐票的数据写进同一张表 | **按模板 ID 分流**。A01/A04/K01 定义各自独立 |

---

## 9. 附录：实测数据一览

### 9.1 患者汇总

| 患者 | 模板 ID | QR 数量 | 总尺寸 | 拼接后 token | 拼接边界 index |
|---|---|---|---|---|---|
| 安达珠美 | LIAJ045-A01-202104 | 6 | 809 | 406 | 39, 101, 169, 219, 352 |
| 平田よし子 | LIAJ045-A01-202104 | 6 | 820 | 406 | 35, 93, 190, 226, 363 |
| 藤岛纱织 | LIAJ045-A01-202104 | 6 | 638 | 406 | 17, 77, 185, 295, 401 |
| 土谷胜 | LIAJ045-A01-202104 | 6 | 740 | 406 | 38, 114, 233, 304, 372 |
| 服部朱理 | LIAJ045-A04-202104 | 6 | 611 | 301 | 20, 86, 98, 181, 274 |
| 塚原明夫 | LIAJ001-K01-202604 | 9 | 719 | 291 | 11, 46, 50, 92, 115, 151, 198, 281 |

### 9.2 每个 QR 的原始 token 数（`split('$').length`）

| 患者 | 各 QR 的原始 token 数 | 合计 | 拼接 | 差 |
|---|---|---|---|---|
| 安达珠美 | 39, 63, 69, 51, 134, 55 | 411 | −5 | **406** |
| 平田よし子 | 35, 59, 98, 37, 138, 44 | 411 | −5 | **406** |
| 藤岛纱织 | 17, 61, 109, 111, 107, 6 | 411 | −5 | **406** |
| 土谷胜 | 38, 77, 120, 72, 69, 35 | 411 | −5 | **406** |
| 服部朱理 | 20, 67, 13, 84, 94, 28 | 306 | −5 | **301** |
| 塚原明夫 | 11, 36, 5, 43, 24, 37, 48, 84, 11 | 299 | −8 | **291** |

> **合计 − (N−1) = 拼接后** 恒成立（每拼接 1 次，token 少 1 个）。

### 9.3 每个 QR 的字符数 / 尺寸

| 患者 | 字符数（各 QR） | 尺寸（各 QR） |
|---|---|---|
| 安达珠美 | 120, 105, 111, 119, 141, 105 | 135, 132, 137, 127, 144, 134 |
| 平田よし子 | 118, 101, 117, 113, 150, 97 | 139, 135, 135, 126, 150, 135 |
| 藤岛纱织 | 81, 108, 110, 110, 111, 59 | 102, 109, 110, 110, 111, 96 |
| 土谷胜 | 120, 113, 124, 101, 109, 87 | 127, 125, 129, 122, 118, 119 |
| 服部朱理 | 84, 105, 56, 105, 110, 68 | 101, 107, 96, 105, 105, 92 |
| 塚原明夫 | 73, 59, 66, 76, 57, 59, 62, 87, 60 | 87, 74, 66, 82, 81, 77, 77, 91, 84 |

**尺寸的离散度平均 4.5%（最大 7.1%）**，字数平均 13.3%（最大 21.2%），token 数平均 47.6%（最大 60.1%）
→ 这就是「分割单位是尺寸」的证据。

---

## 10. 参考：对本原型的适用

当前实现 `main.js`：

| 函数 | 行 | 问题 |
|---|---|---|
| `splitFixedDocQrPreserveEmpty` | 2338 | **逐个 QR 分割** ← 重复行的根本原因 |
| `FIXED_DOC_QR_FIELD_PRESET` | 2300-2316 | 字段定义（`qrSourceId` / `qrDelimiter` / `fetchIndex`） |
| `resolveFixedDocQrFieldConfig` | 2356 | 字段配置解析 |
| `getFixedDocQrFieldPreviewValue` | 2385-2398 | 预览取值 |

**修正方向：** 把 `splitFixedDocQrPreserveEmpty` 改成「先把所有 QR 拼接，再按 `$` 分割」，
字段解析基准从 `(QRソース, 取得位置)` 改为 **拼接后的 token index**。
