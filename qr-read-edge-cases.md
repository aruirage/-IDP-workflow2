# QR 读取（LIAJ 诊断书 A01）边界场景目录

> 依据：`main.js` QR 读取链路 + 本会话实测量化（mock 6 QR / 701 字符 / 407 值）
> 生成方式：deliver-edge-cases 流程（输入校验 → 边界 → 错误态 → 并发 → 恢复 → 优先级）

## 1. Feature Scope

- **链路**：Step2 文本 tab 的 QR 读取条 → 6 个已知区域（QR1–6，y≈85.2 底部）→ 过滤含 `$` 的 payload → `join('')` → `split(/[$^]/)` → 按 `FIXED_DOC_QR_FIELD_NAMES`（407）位置写值 → 门控 → QR 优先路径 / OCR 回退
- **门控（当前）**：`ok = !enabled || valueCount === 407`。6/6 检测数门已废除；「≥5」按用户决定不设硬闸（与开关冗余）
- **区隔符**：`$`（主）+ `^`（副，仅 templateID^患者氏名 一处）
- **空值约定**：空字段 = 连续 `$$` 占位，token 数守恒
- **已知前提**：每 QR 的字段映射是**动态的**（不固定），所以必须「先拼总串再按位置切」；字段值内**不会**出现 `$`/`^`（帐票仕样保证）；真实检测/过滤在产品端已做，demo 是 mock

> ### ⚠️ Demo 关键限制（本次修正）
> `fixedDocQrUndetectedIds` 只有两处写入：① `runFixedDocQrScan` 用「SAMPLE payload 是否含 `$`」静态推导；② 测试脚本手动注入。
> 6 个 sample 全部含 `$` → **demo 每次扫描恒为 6/6 → 恒为 407 值 → `ok` 恒为 true**。
> 因此红标、告警、值数≠407、OCR 回退等**所有非绿路径在 demo 中都无法自然触发**（UI 逻辑存在但不可达）。5/6、0/6 只能靠 console 注入 `fixedDocQrUndetectedIds` 复现。真实扫描器接入后 undetected 才由「定位+复号」结果填充。

## 2. 边界场景分类

### A. 输入校验

| # | 场景 | 现状行为 | 问题/风险 |
|---|---|---|---|
| 1 | 区域在但 payload 为空（复号出空串） | 不含 `$` → 被 `includes('$')` 过滤 → 不进拼接 | 与「该区根本没 QR」无法区分，诊断只有红框，无「有框但复号失败」提示 |
| 2 | payload 只有 `^` 没有 `$` | 被 `includes('$')` 过滤 → 整码丢弃 | 与规则 2「`$` **或** `^`」字面不符（现 mock 数据无此情况） |
| 3 | 字段值含未转义 `$`/`^` | 误切分 → token 数漂移。总数≠407 会被门拦住；相抵成 407 则伪合格 | 用户确认帐票不会出现；当前无任何取值内非法字符校验 |
| 4 | payload 为单个 `$` | split → `["",""]` 两个空 token | token 守恒，行为正确（边界确认项，不需改） |
| 5 | 未传图直接进 Step2 | demo 自动扫描照跑（mock）；真实扫描器「无图」错误路径未定义 | 实机接入时必须补「无图→不扫+提示」 |

### B. 边界条件（实测量化）

| # | 场景 | 值数 | 门控结果 | 说明 |
|---|---|---|---|---|
| 6 | 6/6 | 407 | ✅ QR 优先路径 | 正常 |
| 7 | 5/6（QR3 缺失） | 339（−68） | ❌ 整体 OCR 回退 | ⚠️ demo 无法自然触发（仅测试注入）；跨边界值被截断（`便潜血陽性`→`便潜`），整体拒绝所以不会写错 |
| 8 | 5/6（QR6 缺失） | 353（−54） | ❌ 整体 OCR 回退 | ⚠️ demo 无法自然触发；前 353 值其实全对，但按全有全无整批丢弃（已知限制） |
| 9 | 0/6 | 1（−406） | ❌ OCR 回退 | ⚠️ demo 无法自然触发；6 个红框仍可见，可诊断 |
| 10 | 值数 406 或 408（差 1） | — | ❌ 整体拒绝 | 一个 token 缺失/多余 → 407 值全部作废 |
| 11 | 值数 407 但中段少、后段多（相抵错位） | 407 | ✅ **带错数据通过** | 位置式全局映射无重同步，总门抓不到相抵错位（最危险） |
| 12 | 407 值中大量空值（连续 `$$`） | 407 | ✅ | 正常空字段语义 |

### C. 错误状态

| # | 场景 | 现状 | 风险 |
|---|---|---|---|
| 13 | 单区未检出（QR3） | is-missing 红框 + 告警 + OCR 回退 | ⚠️ UI 逻辑支持，但 demo 无法自然触发（仅测试注入） |
| 14 | 底部无关 QR（误识别） | 检测宇宙被硬编码为 6 已知区 → demo 偶然安全；真实扫描器缺「已知 rect/集合过滤」 | 规则 1 未落地 |
| 15 | 扫描中再点扫描 | `scanActive` 守卫直接 return false | 正确 |
| 16 | 扫描中关开关 | 扫描继续，结果应用后开关已 OFF → `ok=true`（无告警） | 状态轻微不一致，可接受 |
| 17 | 扫描中换图/重传 | 旧图扫描结果可能套到新图上 | 真实扫描器需把结果绑图像 ID |
| 18 | 扫描器 API 失败/超时 | demo 为 mock，无此路径 | 实机必须补错误显示 |
| 19 | Step2 自动扫描（silent）与手动扫描竞争 | scanActive 守卫 | 正确 |

### D. 并发/联动

| # | 场景 | 现状 | 风险 |
|---|---|---|---|
| 20 | 双击扫描 / 自动+手动并发 | 守卫 | 正确 |
| 21 | **帐票字段增删后与 407 门不联动** | `expectedValues` 硬编码 407：删字段 → 该名值落空（门照过）；**加字段 → 门 407===407 照过，但新字段无 QR 值（空/OCR）**，绿标「QR 読取優先パス適用中」仍亮 | **真实漏洞**：门不对齐用户实际字段数，客户会看到绿标+空字段 |
| 22 | 字段顺序调整 | 写值按字段名查（`fixedDocQrValueByName`），非位置 | 安全 |
| 23 | 重复字段名 | Map 首值生效，后者值被丢 | 低风险（schema 生成，无重复） |
| 24 | 段边界缺尾 `$`（某段不以 `$` 结尾） | join 时与前段粘连 → token 合并错位 | mock 数据都带尾 `$`，真实数据无校验「段首/段尾必须是 `$`」 |

### E. 恢复路径

| # | 场景 | 现状 |
|---|---|---|
| 25 | 再扫描 | 清 undetected → 全区重扫，6/6 恢复 |
| 26 | 重传图 | 告警文案提示「赤枠あり→画像再アップロード→再スキャン」 |
| 27 | 关开关 | 显式 OCR only，无静默回退 |

## 3. 错误消息（现状）

- 红色路径标：`QR 不足 — OCR で読取`
- 告警：`QR から読み取った値の数が項目数と一致しません（値 X / 407）。赤枠（未検出 QR）がある場合は画像を再アップロードして再スキャンしてください。`
- 扫描成功 toast：`QR を N / N 検出しました`
- OFF：`無効 — OCR のみで読取`

## 4. 优先级（可能性 × 影响）

| 级别 | 用例 | 理由 |
|---|---|---|
| 🔴 高 | #21 字段增删后门不对齐 | 客户操作即触发，绿标+空字段静默出错 |
| 🔴 高 | #11 相抵错位伪合格 | 静默写错数据（概率低、影响致命） |
| 🔴 高 | #17 换图竞态 | 真实扫描器接入即存在 |
| 🟡 中 | #14 真实扫描误检过滤 | 规则 1，实机接入时必做 |
| 🟡 中 | #2 `^` 过滤缺口 / #1 空 payload 区分 | 规则 2 字面缺口 |
| 🟢 低 | #3 值内 `$`/`^` | 帐票仕样保证不出现；#24 段边界校验 | 防御性 |

## 5. QA 测试场景

1. 6/6 → 407/407 → 绿标（正常）
2. 注入 QR3 缺失 → 红标 + 值 339 + 红框 QR3 + 告警
3. 注入 QR6 缺失 → 值 353，整体拒绝
4. 0/6 → 值 1，OCR 回退
5. 手工构造 406/408 值 → 整体拒绝
6. 开关 OFF → 无 QR 值、`ok=true`、OCR only
7. 双击扫描 → 只跑一次
8. Step2 进入自动扫描后立刻手动点扫描 → 不并发
9. 值内含 `$`（构造）→ 观察是否被误切（当前无防护，记录行为）
10. 字段增删后：新增字段是否空 + 绿标是否仍亮（#21 回归验证）
11. 重扫恢复 6/6 + 绿标

## 6. 未决问题（挂起）

- **`(index):2912 Uncaught (in promise) SyntaxError: Invalid left-hand side in assignment`**：静态排查（node --check 通过、无 eval/new Function、内联脚本空、public 干净）定位不到，疑为浏览器缓存的旧注入版页面或运行时动态执行。需要完整堆栈（触发行 + promise 来源）才能继续。
